import './index.sass'
import React from 'react'
import ReactDOM from 'react-dom'
import SaloScene from "./SaloScene"

ReactDOM.render(
  <React.StrictMode>
    <SaloScene />
  </React.StrictMode>,
  document.getElementById('root')
)
